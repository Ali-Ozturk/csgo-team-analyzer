import React, {useEffect} from 'react';

const TempTest = () => {

    useEffect(() => {
        console.log("ola");
    }, [])

    return (
        <div>
            a
        </div>
    );
};

export default TempTest;
